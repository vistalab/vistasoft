function vol = rxOrthogonalSlice(rx);
%
% vol = rxOrthogonalSlice(rx);
%
% Compute a slice orthogonal to
% the interpolated slice, for 
% viewing the rx placement.
%
% Will rotate the volume along
% the y axis in mrGray conventions
% (as if rotating axials clockwise)
% 90 degrees from the current 
% prescription.
%
%
% ras 03/05.
vol = [];

return
